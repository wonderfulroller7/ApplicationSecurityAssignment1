#include<stdlib.h>
#include "dictionary.h"

int main(int argc, char *argv[]) {

    int exit_status = EXIT_SUCCESS;
    FILE *fp;
    printf("%d\n", argc);
    while(*++argv != NULL) {
        
    }
    return exit_status;
}